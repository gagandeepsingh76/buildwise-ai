# Shared Contracts

This folder contains small, framework-neutral contracts used by both the FastAPI backend and the Next.js frontend.

- `authority_catalog.json` is the reusable jurisdiction registry.
- `api-contract.ts` mirrors public API payloads for frontend typing.
