# Seed Data

Run the migrations in `supabase/migrations` to create the schema and seed the initial authority catalog.

The seed inserts real authority metadata and official links. It deliberately does not invent statutory clauses. Upload official PDFs through the admin panel or ingestion script to populate `document_chunks` with enforceable source text.
