# BuildWise AI Frontend

Next.js App Router frontend for BuildWise AI.

Run locally:

```bash
npm install
npm run dev -- --hostname 127.0.0.1 --port 3000
```

Build:

```bash
npm run lint
npm run build
```
